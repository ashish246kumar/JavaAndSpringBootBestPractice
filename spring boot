.\mvnw clean package -D spring.profiles.active=dev-->it is when we are making the jar and setting the profile
java -jar build/libs/myapplication-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod-->it is when we are running the jarand passing the profile as a argument
 
